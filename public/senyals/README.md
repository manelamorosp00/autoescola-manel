Coloca aquí las imágenes de las señales (mismo nombre que el campo `img` de
cada pregunta en `lib/questions.js`, p. ej. `senyals-reglament-01.svg`).
Mientras no exista el archivo, la app muestra automáticamente un
marcador de posición con el texto descriptivo (`imgAlt`).
